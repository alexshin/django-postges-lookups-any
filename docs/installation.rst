============
Installation
============

At the command line::

    $ easy_install django-postges-lookups-any

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-postges-lookups-any
    $ pip install django-postges-lookups-any
